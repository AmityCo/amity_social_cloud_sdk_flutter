import 'package:amity_sdk/domain/model/amity_post.dart';

class PostComposerUsecase {
  AmityPost excute(AmityPost post) {
    //1. get the data from the diffrent usecase
    //2. Compose them togheter,attach all the public model
    return AmityPost();
  }
}

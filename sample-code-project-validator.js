const path = require('path')
const util = require('util')
const { spawn } = require('child_process');

async function validateSampleCodeProject() {
	// Implement the logic here
	// Throw error if the sample code project is not valid.
}

module.exports.validate = validateSampleCodeProject